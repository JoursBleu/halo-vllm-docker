# Networking (Strix Halo)

Strix Halo is a single-GPU APU mini-PC, so multi-node networking is **out of
scope** for this project. Run in `--solo` mode (see the README). This file is a
placeholder kept only so the docs path is stable if clustering is ever added.
